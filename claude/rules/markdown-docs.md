---
paths: ["**/*.md"]
---

Authoring or restructuring any .md (README, ADR, runbook) - load `markdown-style` first; its
own keywords only catch explicit lint asks, so a content edit misses it. Skip one-line tweaks.
